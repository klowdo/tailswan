# TailSwan Environment Configuration
# Copy this file to .env and fill in your values
#
# IMPORTANT: Never commit your .env file to version control!
# The .env file should contain sensitive credentials like auth keys.

# ==============================================================================
# Tailscale Configuration
# ==============================================================================

# Tailscale authentication key (REQUIRED)
# Get your auth key from: https://login.tailscale.com/admin/settings/keys
# Use an auth key with appropriate expiration and tags for your use case
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx-yyyyyyyyyyyyyyyyyyyyyyyyyyyy

# Hostname for the Tailscale node
# This is how your TailSwan device will appear in your tailnet
# Default: tailswan-gateway
TS_HOSTNAME=tailswan-gateway

# Comma-separated list of subnets to advertise to your Tailscale network
# These should match your IPsec subnet configuration
# Example: 10.1.0.0/24,10.2.0.0/24,192.168.1.0/24
# Default: 10.1.0.0/24,10.2.0.0/24
TS_ROUTES=10.1.0.0/24,10.2.0.0/24

# Enable Tailscale SSH server
# Allows you to SSH into the container via Tailscale
# Options: true, false
# Default: true
TS_SSH=true

# Additional arguments for 'tailscale up' command
# Example: --accept-routes --accept-dns
# Default: (empty)
TS_EXTRA_ARGS=

# Tailscale state directory
# Default: /var/lib/tailscale
TS_STATE_DIR=/var/lib/tailscale

# ==============================================================================
# strongSwan Configuration
# ==============================================================================

# Auto-start IPsec connections on container start
# Options: true, false
# Default: false
SWAN_AUTO_START=false

# Comma-separated list of connection names to auto-start
# Only used if SWAN_AUTO_START=true
# Must match connection names defined in your swanctl.conf
# Example: site-to-site,remote-office,branch1
# Default: (empty - all connections will be started if SWAN_AUTO_START=true)
SWAN_CONNECTIONS=

# Path to swanctl configuration file inside the container
# Default: /etc/swanctl/swanctl.conf
SWAN_CONFIG=/etc/swanctl/swanctl.conf

# ==============================================================================
# Volume Mount Paths
# ==============================================================================

# Path to your swanctl.conf configuration file on the host
# Default: ./swanctl.conf
SWANCTL_CONFIG_PATH=./swanctl.conf

# Certificate paths (uncomment and set if using certificate-based authentication)
# Path to X.509 end entity certificates
# CERTS_X509_PATH=./certs/x509

# Path to X.509 CA certificates
# CERTS_X509CA_PATH=./certs/x509ca

# Path to private keys
# CERTS_PRIVATE_PATH=./certs/private

# ==============================================================================
# Example Configurations
# ==============================================================================

# Site-to-Site VPN with Auto-Start
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx
# TS_HOSTNAME=office-gateway
# TS_ROUTES=10.1.0.0/24,10.2.0.0/24
# SWAN_AUTO_START=true
# SWAN_CONNECTIONS=office-vpn

# Road Warrior / Remote Access VPN
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx
# TS_HOSTNAME=vpn-gateway
# TS_ROUTES=10.0.0.0/8
# SWAN_AUTO_START=false

# Multiple Sites with Certificate Authentication
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx
# TS_HOSTNAME=multi-site-gateway
# TS_ROUTES=10.1.0.0/24,10.2.0.0/24,10.3.0.0/24
# SWAN_AUTO_START=true
# SWAN_CONNECTIONS=site1,site2,site3
# CERTS_X509_PATH=./certs/x509
# CERTS_X509CA_PATH=./certs/x509ca
# CERTS_PRIVATE_PATH=./certs/private
